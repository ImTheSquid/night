---
import Base from "../layouts/Base.astro";
import SetList from "../components/SetList.astro";
import { jackNights } from "../lib/sets";

const sets = await jackNights();
---

<Base title="Jack Night Repository">
  <h1>JACK NIGHT REPOSITORY</h1>
  <span>Looking for Second Fridays? Click <a href="/second-fridays">here</a>.</span>
  <SetList sets={sets} />
</Base>
